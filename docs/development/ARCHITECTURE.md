# Coach AI - System Architecture

Detailed architecture documentation for Coach AI.

---

## 🏛️ System Overview

Coach AI is a cloud-native, microservices-oriented application designed for clinical training through interactive patient simulations.

### High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        User Layer                            │
│  (Medical Students, Educators, Administrators)               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ HTTPS/WSS
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  React SPA (TypeScript)                              │  │
│  │  - Dashboard  - Scenario Player  - Assessment        │  │
│  │  - State Management (Zustand)                        │  │
│  │  - WebSocket Client  - Audio Service                 │  │
│  └──────────────────────────────────────────────────────┘  │
│             Hosted on: Azure Static Web Apps                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ REST API / WebSocket
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   Application Layer                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  FastAPI Backend (Python)                            │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │ API Routes │  │  Services  │  │   Core     │    │  │
│  │  │  - REST    │  │  - Engine  │  │  - Config  │    │  │
│  │  │  - WS      │  │  - Assess  │  │  - Auth    │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│             Hosted on: Azure App Service                    │
└─────┬──────────────────┬──────────────────┬────────────────┘
      │                  │                  │
      │                  │                  │
      ▼                  ▼                  ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐
│  Data Layer │  │   AI Layer  │  │   Storage Layer     │
│             │  │             │  │                     │
│ PostgreSQL  │  │  Azure      │  │  Azure Blob        │
│  Database   │  │  OpenAI     │  │  Storage           │
│             │  │             │  │  (Audio Files)     │
│  - Students │  │  Azure      │  └─────────────────────┘
│  - Scenarios│  │  Speech     │
│  - Sessions │  │  Services   │
│  - Assess.  │  └─────────────┘
└─────────────┘
  Azure Database
  for PostgreSQL
```

---

## 🎯 Architecture Principles

### 1. Cloud-Native Design
- Built for Azure from the ground up
- Horizontal scaling capability
- Managed services for infrastructure
- Stateless application design

### 2. Separation of Concerns
- Clear boundaries between layers
- Business logic isolated in services
- Data access through ORM
- API layer for external communication

### 3. Security First
- Authentication at every layer
- Encrypted data in transit and at rest
- Minimal privilege principle
- Audit logging throughout

### 4. Performance Optimized
- Async/await throughout
- Database query optimization
- Caching strategies
- CDN for static assets

### 5. Scalability
- Horizontal scaling for backend
- Serverless frontend
- Database connection pooling
- Load balancing ready

---

## 📦 Component Architecture

### Frontend Architecture

```
┌─────────────────────────────────────────────────────┐
│                  Browser                             │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │            React Application               │    │
│  │                                            │    │
│  │  ┌──────────────┐  ┌──────────────────┐  │    │
│  │  │  Components  │  │  Services        │  │    │
│  │  │              │  │                  │  │    │
│  │  │  Dashboard   │  │  API Client      │  │    │
│  │  │  Scenarios   │  │  WebSocket       │  │    │
│  │  │  Player      │  │  Audio Service   │  │    │
│  │  │  Assessment  │  │  Auth Service    │  │    │
│  │  └──────────────┘  └──────────────────┘  │    │
│  │                                            │    │
│  │  ┌──────────────┐  ┌──────────────────┐  │    │
│  │  │    Store     │  │     Routing      │  │    │
│  │  │              │  │                  │  │    │
│  │  │  Zustand     │  │  React Router    │  │    │
│  │  │  State Mgmt  │  │  v6              │  │    │
│  │  └──────────────┘  └──────────────────┘  │    │
│  │                                            │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
└─────────────────────────────────────────────────────┘
```

**Component Hierarchy:**

```
App
├── Header
├── Routes
│   ├── Dashboard
│   │   ├── SkillsRadar
│   │   └── ProgressChart
│   ├── ScenarioLibrary
│   │   └── ScenarioCard[]
│   ├── ScenarioPlayer
│   │   ├── PatientInfo
│   │   ├── ChatInterface
│   │   └── AudioInterface
│   └── AssessmentResults
│       ├── SkillsBreakdown
│       └── Feedback
└── Footer
```

### Backend Architecture

```
┌─────────────────────────────────────────────────────────┐
│                FastAPI Application                       │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │              API Layer (Routes)                 │   │
│  │                                                 │   │
│  │  /scenarios  /sessions  /assessments  /voice   │   │
│  │  /analytics  /health                           │   │
│  └───────────────────┬─────────────────────────────┘   │
│                      │                                  │
│                      ▼                                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │           Services Layer                        │   │
│  │                                                 │   │
│  │  ScenarioEngine     AssessmentEngine           │   │
│  │  ClarkIntegration   ClareIntegration           │   │
│  │  VoiceService       AuthService                │   │
│  └───────────────────┬─────────────────────────────┘   │
│                      │                                  │
│                      ▼                                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │           Data Access Layer (ORM)               │   │
│  │                                                 │   │
│  │  SQLAlchemy Models                             │   │
│  │  Student  Scenario  Session  Assessment        │   │
│  └───────────────────┬─────────────────────────────┘   │
│                      │                                  │
│                      ▼                                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Core Layer                         │   │
│  │                                                 │   │
│  │  Config  Database  Security  Azure Services    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 💾 Data Architecture

### Database Schema

```
┌─────────────┐
│  students   │
│─────────────│
│ id (PK)     │─┐
│ email       │ │
│ full_name   │ │
│ institution │ │
└─────────────┘ │
                │
                │  1:N
                │
         ┌──────┴──────┐
         │             │
         ▼             ▼
┌─────────────┐  ┌─────────────┐
│  sessions   │  │ assessments │
│─────────────│  │─────────────│
│ id (PK)     │  │ id (PK)     │
│ session_id  │──│ session_id  │
│ student_id  │  │ student_id  │
│ scenario_id │  │ scores...   │
│ transcript  │  └─────────────┘
│ status      │
└─────────────┘
       │
       │ N:1
       │
       ▼
┌─────────────┐
│  scenarios  │
│─────────────│
│ id (PK)     │
│ scenario_id │
│ title       │
│ specialty   │
│ dialogue... │
│ rubric...   │
└─────────────┘
```

### Data Flow

**Scenario Execution:**

```
1. Student selects scenario
   └─> GET /scenarios/{id}

2. Create session
   └─> POST /sessions

3. WebSocket connection
   └─> WS /ws/{session_id}

4. Real-time dialogue
   ├─> Student sends message
   │   └─> Scenario Engine processes
   │       └─> OpenAI generates response
   │           └─> Speech Service creates audio
   │               └─> Response sent to client
   │
   └─> Loop until completion

5. Complete session
   └─> POST /sessions/{id}/complete

6. Generate assessment
   └─> POST /assessments
       └─> Assessment Engine calculates scores
           └─> Save to database

7. Display results
   └─> GET /assessments/session/{id}
```

---

## 🔄 Process Flows

### Scenario Creation Flow

```
┌──────────┐
│ Educator │
└─────┬────┘
      │
      │ 1. Create Scenario
      ▼
┌─────────────────┐
│  POST /scenarios│
│  (Draft Status) │
└────────┬────────┘
         │
         │ 2. Auto-enrich
         ▼
┌─────────────────┐
│ Clare Service   │
│ Fetch Guidelines│
└────────┬────────┘
         │
         │ 3. Review
         ▼
┌─────────────────┐
│ Human Reviewer  │
│ Quality Check   │
└────────┬────────┘
         │
         │ 4. Approve
         ▼
┌─────────────────┐
│ POST /scenarios │
│ /{id}/publish   │
└────────┬────────┘
         │
         │ 5. Available
         ▼
┌─────────────────┐
│ Students can    │
│ access scenario │
└─────────────────┘
```

### Assessment Flow

```
┌──────────────┐
│ Session Data │
└──────┬───────┘
       │
       │ Input
       ▼
┌─────────────────────────┐
│  Assessment Engine      │
│                         │
│  1. History Taking      │───> Score (0-100)
│     - Coverage Analysis │
│     - Relevance Check   │
│                         │
│  2. Clinical Reasoning  │───> Score (0-100)
│     - Red Flag ID       │
│     - Diagnosis Check   │
│                         │
│  3. Management          │───> Score (0-100)
│     - Plan Appropriate  │
│                         │
│  4. Communication       │───> Score (0-100)
│     - Question Quality  │
│                         │
│  5. Efficiency          │───> Score (0-100)
│     - Time Management   │
│                         │
└─────────┬───────────────┘
          │
          │ Weighted Average
          ▼
    ┌─────────────┐
    │ Overall     │
    │ Score       │
    │ (0-100)     │
    └─────────────┘
```

---

## 🔌 Integration Architecture

### External Services

```
┌─────────────────────────────────────────────┐
│         Coach AI Application                │
└───┬──────────┬──────────┬──────────┬───────┘
    │          │          │          │
    │          │          │          │
    ▼          ▼          ▼          ▼
┌─────────┐ ┌──────┐ ┌──────┐ ┌──────────┐
│  Clare  │ │Clark │ │Azure │ │  Azure   │
│   API   │ │ API  │ │OpenAI│ │  Speech  │
└─────────┘ └──────┘ └──────┘ └──────────┘
    │          │         │          │
    │          │         │          │
    ▼          ▼         ▼          ▼
┌────────────────────────────────────────┐
│     Guidelines   Cases   AI   Voice    │
└────────────────────────────────────────┘
```

**Integration Points:**

1. **Clare Integration**
   - Fetch clinical guidelines
   - Link to scenarios
   - Provide references in feedback

2. **Clark Integration**
   - Import anonymized consultations
   - Convert to scenarios
   - Extract patient profiles

3. **Azure OpenAI**
   - Generate patient responses
   - Context-aware dialogue
   - Natural language processing

4. **Azure Speech**
   - Text-to-speech synthesis
   - Multiple voice options
   - Emotional styles

---

## 🚀 Deployment Architecture

### Development Environment

```
┌─────────────────────────────────────┐
│        Developer Machine            │
│                                     │
│  ┌──────────┐  ┌──────────┐       │
│  │ Frontend │  │ Backend  │       │
│  │  :3000   │  │  :8000   │       │
│  └──────────┘  └──────────┘       │
│                      │              │
│                      ▼              │
│              ┌──────────┐          │
│              │PostgreSQL│          │
│              │  :5432   │          │
│              └──────────┘          │
└─────────────────────────────────────┘
```

### Production Environment (Azure)

```
┌─────────────────────────────────────────────┐
│            Azure Cloud                      │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │  Azure Front Door (CDN)              │  │
│  │  - SSL Termination                   │  │
│  │  - WAF                               │  │
│  └──────────┬─────────────┬─────────────┘  │
│             │             │                 │
│             ▼             ▼                 │
│  ┌──────────────┐  ┌────────────────────┐  │
│  │ Static Web   │  │  App Service       │  │
│  │ Apps         │  │  (Backend)         │  │
│  │ (Frontend)   │  │  - Auto Scale      │  │
│  └──────────────┘  │  - Load Balanced   │  │
│                    └─────────┬──────────┘  │
│                              │              │
│                              ▼              │
│  ┌──────────────────────────────────────┐  │
│  │    Azure Database for PostgreSQL    │  │
│  │    - Backup Enabled                 │  │
│  │    - High Availability              │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────┐  ┌──────────────────┐   │
│  │ Blob Storage │  │  Key Vault       │   │
│  │ (Audio)      │  │  (Secrets)       │   │
│  └──────────────┘  └──────────────────┘   │
│                                             │
│  ┌──────────────┐  ┌──────────────────┐   │
│  │ OpenAI       │  │  Speech Services │   │
│  │ Service      │  │                  │   │
│  └──────────────┘  └──────────────────┘   │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │   Application Insights               │  │
│  │   - Monitoring                       │  │
│  │   - Logging                          │  │
│  │   - Analytics                        │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

---

## 🔒 Security Architecture

### Authentication Flow

```
┌────────┐
│ User   │
└───┬────┘
    │ 1. Login
    ▼
┌────────────┐
│ Azure AD   │
│ B2C        │
└─────┬──────┘
      │ 2. Token
      ▼
┌────────────┐
│ Frontend   │
└─────┬──────┘
      │ 3. API Request
      │    + Bearer Token
      ▼
┌────────────┐
│ Backend    │
│ Verify JWT │
└─────┬──────┘
      │ 4. Access Resource
      ▼
┌────────────┐
│ Database   │
└────────────┘
```

### Security Layers

```
┌─────────────────────────────────────────┐
│         Application Layer               │
│  - Input Validation                     │
│  - SQL Injection Prevention (ORM)       │
│  - XSS Prevention (React)               │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│         Network Layer                   │
│  - HTTPS Only                           │
│  - WAF (Web Application Firewall)       │
│  - DDoS Protection                      │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│         Data Layer                      │
│  - Encryption at Rest                   │
│  - Encryption in Transit                │
│  - Row-Level Security                   │
└─────────────────────────────────────────┘
```

---

## 📊 Monitoring & Observability

```
Application
    │
    ├─> Logs ────────┐
    ├─> Metrics ─────┤
    ├─> Traces ──────┤
    └─> Events ──────┤
                     ▼
         ┌────────────────────┐
         │ Application        │
         │ Insights           │
         └────────┬───────────┘
                  │
                  ▼
         ┌────────────────────┐
         │ Azure Monitor      │
         │ - Dashboards       │
         │ - Alerts           │
         │ - Analytics        │
         └────────────────────┘
```

---

## 🎯 Future Architecture

### Planned Enhancements

1. **Microservices Split:**
   - Voice Service (separate)
   - Assessment Service (separate)
   - Notification Service

2. **Event-Driven Architecture:**
   - Azure Service Bus for async processing
   - Event Grid for integrations

3. **Enhanced Caching:**
   - Azure Redis Cache
   - CDN for API responses

4. **Advanced Analytics:**
   - Azure Synapse for data warehouse
   - Power BI for insights

---

## 📝 Architecture Decisions

### Why FastAPI?
- Modern async support
- Automatic API docs
- Type safety with Pydantic
- WebSocket support
- High performance

### Why React?
- Component reusability
- Large ecosystem
- TypeScript support
- Excellent tooling
- Azure Static Web Apps compatibility

### Why PostgreSQL?
- JSONB for flexible data
- Strong consistency
- Azure managed service
- Advanced querying
- ACID compliance

### Why Azure?
- Enterprise-ready
- HIPAA compliance
- UK data residency
- Comprehensive AI services
- Integrated monitoring

---

This architecture is designed to be scalable, secure, and maintainable for the long term.
